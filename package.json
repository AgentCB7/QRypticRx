{
  "name": "qrypticrx",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n BE,FE -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently -n BE,FE -c blue,magenta \"npm start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
